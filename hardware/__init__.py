"""Hardware / SITL interface layer (MAVLink).

- ``mavlink_driver`` — lateral guidance driver (RC override CH1), reuses the
  controllers from the :mod:`mpc_nav` package.
- ``rtl_monitor`` — standalone RTL / loiter cross-track monitor.
"""
